<Window x:Class="Mp3TagUI.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://www.codeplex.com/prism"
        xmlns:inf="clr-namespace:Mp3.Infrastructure;assembly=Mp3.Infrastructure"    
        xmlns:view="clr-namespace:Mp3TagUI;assembly=Mp3TagUI"
        Title="Tag Updater" Height="350" Width="425">
    <Window.Resources>
        <Style x:Key="MainBackgroundStyle">
            <Setter Property="Control.Background">
                <Setter.Value>
                    <SolidColorBrush Color="Orange"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Style="{StaticResource MainBackgroundStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <ItemsControl Grid.Row="0" prism:RegionManager.RegionName="{x:Static inf:RegionNames.Main}" Margin="10,10,10,10" Background="Black" Foreground="#FFF9F7F7" ></ItemsControl>
        <ContentControl Grid.Row="1" prism:RegionManager.RegionName="{x:Static inf:RegionNames.Log}" Margin="10,10,10,10" ></ContentControl>
        <ContentControl Grid.Row="2" prism:RegionManager.RegionName="{x:Static inf:RegionNames.Progress}" ></ContentControl>
    </Grid>
</Window>
